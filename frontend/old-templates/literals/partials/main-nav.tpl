<nav>
	<a href="/articles/"${d.navActive === 'articles' ? ` class="active"` : ``}>
		Articles
	</a>
	<a href="/settings/"${d.navActive === 'settings' ? ` class="active"` : ``}>
		Settings
	</a>
</nav>
<div>
	
</div>
